<div class="card">
  <h5 class="card-header"><%= @secret.title %></h5>
  <div class="card-body">
    <p class="card-text"><%= simple_format(@secret.body) %></p>
  </div>
  <div class="card-footer text-muted"><strong>This secret has been destroyed.</div>
</div>

<br/>

<div class="actions">
  <%= link_to 'Back', secrets_path, class: "btn btn-primary" %>
</div>
